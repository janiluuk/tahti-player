---
description: After plugin/theme add or change, update janiluuk/tahti-registry (sibling ../tahti-registry).
globs: "**/plugins/**"
alwaysApply: false
---

# tahti-registry

Store catalog = `../tahti-registry` (not on-disk runtime `plugins.json`). After add/change/remove: update catalog version/downloadUrl; run `pnpm validate` / `pnpm check-plugins`.

Full checklist: `docs/agent/REGISTRY.md`.
