# docs/todo — open work index

Agents: read this file instead of listing or skimming all of `docs/todo/`.  
When a task is finished → fold into [`HISTORY.md`](HISTORY.md), delete the todo file, update this index, and remove finished checkboxes from `WORKPLAN.md` / other active trackers.

Status values: `open` | `blocked` | `partial`.

| Status  | File                                                                                                     | One-line                                                                                                                                                                                                                                                       |
| ------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| partial | [radio-casting-broken.md](radio-casting-broken.md) | tahti-web + AirPlay played no sound (tracks and radio): Web Audio capture for visualizers. Fixed 2026-09-25 by not capturing in AirPlay-capable browsers plus native HLS; needs a real-device AirPlay check |
| open    | [player-performance-optimizations.md](player-performance-optimizations.md)                               | (Later) Release-build CPU/native-memory profiling, then startup, queue, playback, visualizer and loading optimizations                                                                                                                                         |
| open    | [ai-dj-radio-dj-port.md](ai-dj-radio-dj-port.md)                                                          | **POC/beta, not committed** — plan to trial AI-DJ talk-breaks (from `../radio-dj`) on `tahti-radio`/`tahti-selects` using the LocalAI cluster (192.168.2.101-105); Stage 1 is offline-only content review before any production wiring, kill-switched by default in Stage 2 |
| partial | [desktop-pro-library.md](desktop-pro-library.md)                                                         | Phase 0 fully done; Phase 1 playback, filename migration, recursive folder import, native drag-drop, import progress/cancellation/retry, and reveal-in-folder now work end to end. 2026-09-21: library roots (scan/new-file discovery/folder relink), FTS5 search, batched import and a read cache shipped; open in Phase 1: full metadata extraction, resumable indexing, iTunes XML import, filesystem watching (Phase 6). Phase 2 (browse, sortable virtualized table, filters, select-all, keyboard, inspector, saved layouts, view retention) complete 2026-09-21; Phase 3 (local playlists: create/rename/duplicate/delete, add from selection/results/groups/queue, reorder, undo, play/shuffle, M3U/M3U8 import and export, queue persisted and restored paused) complete 2026-09-21; Phase 4 (metadata edits with overrides/undo/preview, ratings/labels/tags/play counts, exact and similar duplicate review, catalog backup/restore with root remapping) incl. recoverable write-tags-to-files for FLAC/WAV, duplicate merge, local listening history) complete 2026-09-21; Phase 5 (background analysis: waveform, loudness/true peak, BPM, key; corrections; BPM/key/loudness filters and sorts; smart playlists) built 2026-09-21, still needing real-music accuracy validation and normalization playback (optional); Phases 6-7 untouched |
| open    | [import-provider-set-to-native-library.md](import-provider-set-to-native-library.md) | Import every eligible track from a hearthis.at/SoundCloud playlist or set, download through the native layer, and register completed files as durable native-library tracks with progress, retry and deduplication |
| partial | [atlas-navigation-structure-widget.md](atlas-navigation-structure-widget.md)                             | Atlas navigation draft widget supports persisted reorder/add/remove; runtime wiring intentionally deferred                                                                                                                                                     |
| partial | [admin-plugin-management-panel.md](admin-plugin-management-panel.md)                                     | 2026-09-21: user overrode the "no unified page needed" recommendation — wants full submission/moderation parity across all 13 plugin categories. Scoped out as its own multi-phase project (schema design + 10 new submission surfaces + registry integration), not yet started. Bundle upload/publish UI for the existing discovery+channel page also still open 2026-09-25: user-facing Add-ons browser regrouped (user decision 2026-09-18) into Listener/Artist/Admin audience lists with role gating and old category links mapped (Themes lives in Settings). |
| partial | [release-player-workflow-broken.md](release-player-workflow-broken.md)                                   | 2026-09-25: real blocker was GITHUB_TOKEN tag pushes never triggering it; Bump & Tag now calls it directly, plus manual dispatch for existing tags. Plugin SDK release made manually runnable (GitHub Release tarball, npm only with `NPM_TOKEN`). Snap release wired into Release Player (store publish needs `SNAPCRAFT_STORE_CREDENTIALS`). Left: first real run after merge; AUR/Flathub/WinGet still keyed to `workflow_run` |
| partial | [tahti-cli-tool.md](tahti-cli-tool.md)                                                                   | 2026-09-18: v1 shipped — `packages/tahti-cli`, `tahti library list` via plain `fetch` + the existing personal-API-token auth (`TAHTI_API_TOKEN`), not the private cross-repo `@tahti/api-client` the original ask assumed. Playback/TUI still a stretch goal, not started |
| partial | [codebase-refactor-hotspots.md](codebase-refactor-hotspots.md)                                           | PluginStorePanel, SettingsPanels, client.ts, admin.ts, api/studio.ts, router.tsx, ArtistView.tsx, ChannelView.tsx, ChannelDesigner.tsx (2026-09-22), and TrackEditDialog/LocalPlaylists/StreamManagerPanel/AdminStorageView (2026-09-23) all fully done, off the backlog; remaining P2/P3 rows are deliberately deferred (split only if pain appears) |
| partial | [radio-channel-page.md](radio-channel-page.md)                                                           | 2026-09-21: prod data fix applied directly (tahti-radio's channelKind is now RADIO, confirmed live) via granted SSH access; the migration-history baseline loop (exact commands in the doc) was blocked by Claude Code's own safety classifier as a bulk shared-resource change — still needs the user to run it |
| partial | [desktop-ui-slowness.md](desktop-ui-slowness.md) | 2026-09-21: desktop app scrolling/menus feel slow (software-rendered WebKit on NVIDIA + always-on animated background); 30 fps/no-rebuild fix shipped but unmeasured — waiting on the user's manual test results |
| partial | [snapshot-digest-shows-history.md](snapshot-digest-shows-history.md) | 2026-09-21: digest now lists only this PR's new mismatches (master baseline fetched in CI) and the stale `HistoryRow` snapshot is fixed; stale digest comments are cleared when tests pass, `coverage.yml` done too; needs a real PR run to verify |
| open    | [listening-party.md](listening-party.md) | Scheduled album playback from start to finish, full tracklist and liner notes, artist/fan live chat, and direct pre-orders or purchases in all available formats |
| partial | [api-slow-requests.md](api-slow-requests.md) | 2026-09-25: root cause found — uncached health/status dependency fan-out (`/api/v1/status`, `/health`) re-probing Postgres/Redis/MinIO/Centrifugo/Icecast on every call, not DB/MinIO; caching mature elsewhere. Fix needs `../tahti-org` edits + user go-ahead; still needs real Loki evidence to confirm volume |
| open    | [nuclear-queue-bar-port.md](nuclear-queue-bar-port.md) | Port the Nuclear desktop queue bar (right sidebar) into tahti-web, replacing the current tabbed right rail; shared `@tahti-player/ui` components with Storybook stories |
| open    | [audio-editor-waveform-screenshot.md](audio-editor-waveform-screenshot.md) | Load a ~30 s WAV into the audio editor and capture it with the waveform loaded (dark + light) to show the detailed waveform rendering |
| partial | [god-module-restructure.md](god-module-restructure.md) | All non-data app modules from the original size sweep are now split and audited. 2026-09-24: `DesktopLibraryPanel` completed its final presentation/dialog extraction (715→479; full web suite green). 2026-09-25: Rust `local_library/tests.rs` split into per-module test files (lib tests match master: 215 pass, the same 2 macOS-only failures). `<Link><Button/></Link>` nesting removed via new `ButtonLink`/`ButtonAnchor` (92 sites). Remaining: visual checks of signed-in pages that need the running stack. |

## Fold rule (copy into CLAUDE / chat)

```
Done task → append summary to docs/todo/HISTORY.md → delete docs/todo/<file>.md
         → remove row from INDEX.md → strip finished items from WORKPLAN.md
Never leave **Status:** done files in docs/todo/.
```
