# Pro desktop music player — phased implementation workplan

**Status:** partial
**Updated:** 2026-09-21
**Goal:** Import a personal music collection once, then catalog, sort, filter, search, analyze, play and build playlists offline as in a desktop music manager.

This is the canonical implementation plan for the independent player roadmap. It supersedes the old A–F ordering and right-rail library proposal. Historical context: `packages/tahti-web/UI-REDESIGN-WORKLOG.md`, 2026-09-04 — Independent desktop player.

## Verified starting point

- `packages/player/src/main.tsx` mounts `packages/tahti-web/src/TahtiApp.tsx`; desktop and web share frontend code.
- `/library/local` renders `DesktopLibraryPanel` inside LibraryView. The library was moved out of the right rail. Keep the main Library workspace for the catalog; use a rail only for optional details/queue.
- `localLibraryStore.ts` imports browser File objects, creates blob URLs, and persists filename/title/artist only. Playback works in-session; restart requires re-import. Identity restoration currently matches filename, which cannot distinguish same-named files in different folders.
- Tags, durable filesystem references, native catalog queries, analysis and durable local playlists are not implemented by this store.
- `SaveQueueAsPlaylistDialog.tsx` saves Tahti sound IDs to server collections and excludes local tracks. It cannot serve as local playlist persistence.
- Native SQLite infrastructure already exists in `packages/player/src-tauri/src/db.rs` (sqlx/WAL) for history. Reuse conventions, with dedicated versioned catalog migrations.
- Tahti window branding exists; custom window-frame defaults and installer naming are separate follow-ups, not prerequisites for catalog delivery.

## Product and architecture defaults

The desktop library works without a Tahti login or network. Import references original files by default; it does not upload, move or rewrite them. A future managed-copy option must be explicit. Cloud Sounds/Collections remain available alongside the local catalog, with clear source labels.

Use a native SQLite catalog as the durable source of truth; React/Zustand holds view state and bounded query results. Native background jobs handle scan, tag extraction and analysis. IPC returns typed pages/progress, not a whole decoded collection. Browser file preview may remain available but must clearly describe its session-only persistence. Native features use actual runtime capabilities, never screen width as a proxy.

Proposed entities (design targets, not existing contracts): library roots; tracks with stable IDs; file locations with size/mtime and optional content hash; artist/album/genre relations; user metadata overrides; artwork cache; versioned analysis results; ordered playlist entries with their own IDs; saved query rules; resumable jobs. A track can have multiple locations; two files with matching names or tags are not automatically the same track. Playlist references survive relocations.

## Current implementation slice — 2026-09-10

Native side only, not yet wired to the frontend. `packages/player/src-tauri/src/local_library/` has a `library_tracks` SQLite table (migration `0001_init.sql`), FLAC/WAV tag/duration/format extraction via `symphonia` (`metadata.rs`), and four typed commands (`library_list`, `library_import` — native file-picker import, upsert-on-reimport by path, `library_resolve` — existence check + asset-protocol allow, `library_remove`). 8 Rust unit tests cover import, re-import dedup, search (incl. SQL-wildcard escaping), missing-file resolve, and remove, all green; TS bindings regenerated into `packages/player/src/services/tauri/bindings.ts`. Found and fixed in this pass: a real tag-parsing bug where RIFF `LIST`/INFO values (title/artist/album) kept a trailing NUL byte because `symphonia-metadata`'s `riff::parse` includes the terminator in `Tag::value`.

The shared frontend now receives a native-capability signal and typed library
adapter from the Tauri player. Desktop Local files can import through the
native picker, query the SQLite catalog, resolve asset URLs for playback, and
remove catalog entries with paged loading; the browser File API remains the
web fallback. Root scanning, reveal-in-folder, progress/cancellation, and native
E2E coverage remain open. Phase 0's signed-out-access item and most of
Phase 1 remain open.

## 2026-09-12: parallel web-track work — reconciliation needed

A separate branch (this session) built a **browser-only** track in parallel,
unaware of the native catalog work above — picked up before this doc's
"supersedes the old A–F ordering" rewrite landed. Reconciled by keeping the
native plan above as canonical and folding in only what's still additive:

**Kept (merges cleanly, no native dependency):**
- `@tahti-player/ui`'s `TrackTable.features.selectable` is now a **real,
  working feature** (it was a declared-but-unimplemented stub before this
  pass): `useRowSelection` hook, a checkbox column
  (`SelectCell`/`SelectAllHeader`, tri-state select-all), and a bulk-action
  toolbar (N selected / clear / bulk remove + add-to-queue) gated on new
  `actions.onRemoveSelected`/`onAddSelectedToQueue` callbacks. This is
  exactly the "multi-select" half of Phase 2's `[ ] Build a virtualized
  multi-select TrackTable...` checkbox — reusable once Phase 2 wires
  `TrackTable` to the native catalog; it doesn't care where `tracks` comes
  from.
- `PlayableTrackTable` (the `TahtiPlayable`-facing wrapper around
  `TrackTable`) gained `onRemove` and `selectable`/`onBulkRemove` props to
  expose the above. No existing caller needed per-row delete before this.

**Reverted for now (real, tested, but redundant with/superseded by the
native plan — recoverable from commit `0017ae7c` on `master`'s history if
ever useful for the browser-fallback path specifically):**
- Real ID3/Vorbis/MP4 tag extraction in `localLibraryStore.ts` via the
  `music-metadata` npm package (browser-core entry, no Node/WASM deps) —
  async `addFiles`, `album`/`year`/`genre`/`trackNo`/`durationSec` fields,
  non-persisted `artworkUrl`. Native tag extraction already exists and is
  tested (`symphonia`-based, see "Current implementation slice" above), so
  this would only matter for the browser-only fallback path (no Tauri) —
  not reinstated now since Phase 1's `[ ] Extract title, artists, ...`
  checkbox is scoped to the native importer. Worth revisiting only if the
  web-only fallback needs real tags of its own, separate from native.
- `DesktopLibraryPanel` rendering local tracks through `PlayableTrackTable`
  instead of a plain list — superseded by this doc's own native-aware
  rewrite of that same component (native-capability signal, typed library
  adapter, paged native queries). Re-doing this properly means wiring
  `PlayableTrackTable`/`TrackTable` (kept, see above) to the native/browser
  adapter data, which is what Phase 2 already calls for.

Net: nothing net-new was lost — the generic `TrackTable` selection feature
is real and kept; the tag-extraction and panel-rendering pieces are proven
working code sitting in `master`'s history at `0017ae7c`, ready to inform
Phase 1/2 rather than being redone from scratch, whenever those phases are
actually picked up.

## Phase 0 — Native foundation and delivery contract

Depends on: none. Produces the smallest complete desktop catalog boundary.

- [x] Confirm active runtime, native command registration, permissions, app data directory and current auth redirects. Make local-library navigation usable signed out/offline without exposing authenticated cloud operations. — **2026-09-12:** confirmed native runtime (`lib.rs` registers `library_list`/`library_import`/`library_resolve`/`library_remove`, `LibraryState` managed, app-data-dir-relative `databases/library.db`). Found and fixed the actual gap: `AppShell.tsx`'s `ANONYMOUS_ALLOWED_ROUTES` allow-list was missing `/library/local`, so a signed-out user hitting that tab was bounced into the login modal by the `anonymousRouteBlocked` effect even though `DesktopLibraryPanel`/`localLibraryStore`/the native adapter never call any authenticated endpoint. Added `/^\/library\/local(?:\/|$)/` to the allow-list. No other auth redirect touches this route.
- [x] Define a typed native library adapter for capabilities, import jobs, paged queries, details, source resolution and cancellation. — **Already existed** (`lib/nativeCapabilities.ts`, `lib/nativeLibrary.ts`, wired in `packages/player/src/main.tsx`) covering capabilities/list/import/resolve/remove; the browser path (`localLibraryStore.ts`) is a structurally separate File-API fallback rather than a shared adapter that silently no-ops, which already satisfies "reports unsupported honestly" (browser code never invokes Tauri commands). **Not extended this pass:** a `details`/single-track command and formal cancellation token were left out — adding a new Tauri command would change `packages/player/src/services/tauri/bindings.ts`, which is machine-generated by launching the actual Tauri app (`cargo run`, needs a display/webview); this sandboxed environment has no Rust toolchain at all (`cargo`/`rustc` not on PATH) and no GUI, so a hand-edit to a "do not edit manually" generated file couldn't be verified. Left for whoever can run the app locally.
- [x] Add catalog schema/migrations and transactional repository; define stable IDs, file-location identity, metadata precedence, missing-file states and migration/backup policy. — Stable IDs, file-location identity, and metadata precedence already existed (see above). **2026-09-16: missing-file state + migration/backup policy shipped** — a real Rust toolchain is now available in this environment (`cargo`/`rustc` on PATH; the older "no toolchain" caveats throughout this doc are now obsolete for anything that doesn't need a display, i.e. everything except `cargo run`/the full GUI). Added migration `local_library/../../migrations/library/0002_availability.sql`: `available INTEGER NOT NULL DEFAULT 1` + `unavailable_since TEXT` columns (additive, `DEFAULT` so the existing `seed_generated_rows` raw INSERT in tests.rs keeps compiling unmodified) plus an index on `available`. `resolve_path` now persists the state via a new `mark_availability()` helper instead of only returning a transient error: marks missing on a failed existence check, self-heals back to available on a later successful resolve (reconnected drive / restored file), and `import_paths`'s upsert also clears the state on (re)import since a successful import proves the file exists right now. Added `list_unavailable()` as the query primitive Phase 1's relink UI will need (not wired to a Tauri command yet — deliberately, see below). Migration/backup policy is now a doc comment on `local_library::open()` (no existing ARCHITECTURE.md/README convention in `src-tauri` to put it in instead): migrations are forward-only sqlx files tracked in-DB, no down-migration story by design; backup/restore is explicitly deferred to Phase 4, noted so a future migration author doesn't invent an ad hoc rollback.
- [x] Build deterministic fixture collections: tagged/untagged files, Unicode paths, duplicate filenames, corrupt files, multiple formats and disconnected roots. Add generated metadata fixtures at 1k/10k/100k rows. — **2026-09-12:** added to `local_library/tests.rs`: untagged WAV (filename fallback), corrupt-file-with-valid-extension (both standalone and mixed into a batch with a good file), Unicode path (CJK + accents + emoji, in both directory and filename), duplicate filename in different folders (distinct tracks, not clobbered), and a disconnected-root simulation (removing a subdirectory leaves the rest of the catalog resolvable). Added a `seed_generated_rows` synthetic-row helper (bypasses audio decode) exercised at 1k and 10k rows for pagination/search; a 100k-row variant exists but is `#[ignore]`d by default to keep the default `cargo test` run fast — run explicitly with `cargo test -- --ignored paginates_100k_generated_rows`. **Not covered:** a real FLAC fixture (only WAV is hand-constructed in tests; symphonia's declared FLAC support has no test of its own — writing a valid FLAC file by hand without an encoder crate wasn't attempted this pass).
- [ ] Establish release-build CPU/native-memory baseline using [performance workplan](player-performance-optimizations.md). Record hardware and initial query/import budgets; measure native host and WebView process tree separately from JS heap. — **Explicitly out of scope this pass** (user-confirmed): requires a real release-build Tauri run on physical hardware with flamegraph/RSS/PSS tooling; not attempted, no numbers fabricated. See `player-performance-optimizations.md`.

**Phase 0 is now fully complete except the explicitly-out-of-scope performance baseline above**, which needs physical hardware, not an environment capability gap.

**2026-09-16 toolchain update:** a real Rust toolchain (`cargo`/`rustc` 1.98.0) is now on PATH in this environment — `cargo test`/`cargo build`/`cargo clippy` all work (verified: 19/20 `local_library` tests pass, 1 intentionally `#[ignore]`d; `cargo clippy --lib` clean except 4 pre-existing warnings unrelated to this module, in `mpd/protocol.rs`/`bridge/mod.rs`/`lib.rs`). Only `cargo run`/the full Tauri GUI still doesn't work headlessly (needs a display/webview; no `Xvfb` available here either) — this specifically blocks regenerating `packages/player/src/services/tauri/bindings.ts`, which is exported from inside `run()` on `cargo run`, not from any test. For this pass's two new `LibraryTrack` fields (`available`, `unavailableSince`), hand-edited `bindings.ts` to match the exact existing style instead (camelCase, `T | null` for `Option<T>`) since nothing generates it here — **whoever can run the app locally should regenerate it for real at some point to confirm the hand-edit matches** what specta would actually emit, though `tsc --noEmit` in both `packages/player` and `packages/tahti-web` is clean against the hand-edited version, and the only frontend consumer (`packages/player/src/main.tsx`'s `nativeLibrary` adapter) maps into a narrower, independently-declared `NativeLibraryTrack` shape that doesn't need the new fields, so nothing downstream depends on the hand-edit being pixel-perfect yet.

Also added: a repository-level `list_unavailable()` query (missing tracks, most-recent-first) as the primitive Phase 1's re-scan/relink UI will need — deliberately **not** exposed as a new `#[tauri::command]` yet, to avoid needing a `bindings.ts` regeneration for a command with no UI caller yet; add the command wrapper when Phase 1 actually wires a relink screen to it.

**Verification note:** `pnpm --filter @tahti-player/tahti-web test` still has the same pre-existing jsdom/localStorage environment failures noted in earlier passes, unrelated to anything touched here.

**Exit demo:** signed-out desktop opens Local files offline (fixed earlier pass); catalog migrations survive restart, now including the availability columns; a typed query returns a fixture page; browser does not invoke native commands; a track resolved after its file goes missing is now persisted as unavailable and self-heals once the file returns (new this pass, `cargo test` verified).

## Phase 1 — Durable import and reliable playback

Depends on: phase 0. First usable personal music library.

- [x] Add files, recursive folder selection and native file/folder drag-drop. Reference originals by default. Show scan progress, imported/skipped/failed counts, cancellation, retry and per-file errors. - **2026-09-18 partial:** added a native recursive folder picker and `Import folder` action. It walks regular files under the selected directory in stable path order, imports case-insensitive FLAC/WAV files through the existing per-file importer, ignores unsupported files, and does not follow directory symlinks. Existing per-file errors remain surfaced in the UI. Native drag-drop, scan progress, cancellation, retry, skipped counts, and background jobs remain open. **2026-09-21 closed out:** added native drag-drop — `getCurrentWebview().onDragDropEvent` (packages/player/src/main.tsx) feeds dropped file/folder paths into a new `library_import_paths` Tauri command (mod.rs), which walks any dropped directories the same way folder import does and imports the rest directly, no dialog needed. Added live scan progress: `import_paths_with_progress` emits a `library://import-progress` event after every file (done/total/imported/failed/skipped/currentPath), rendered in `DesktopLibraryPanel` as a status line ("Importing N of M — X imported, Y failed, Z skipped") plus a Cancel button. Cancellation is cooperative via an `AtomicBool` on `LibraryState`, checked once per file (`library_import_cancel` command) — a file already in flight still finishes. `ImportResult` gained `skipped` (files walked but not FLAC/WAV, previously silently dropped with no count) and `cancelled` fields. Retry: failed imports' paths are kept and offered as a "Retry" action button directly on the error toast (sonner's built-in toast action), resubmitted through the same `importPaths` path. No new background-job queue was introduced — imports still run as one Tauri command invocation per action (files/folder/paths), now with progress streamed out of that single call rather than run as a separate persisted job; this matches the scope of "show progress/counts/cancellation for an import," not Phase 6's job-queue/root-watching work. Verified: `cargo test --lib local_library` 25/25 (1 new test, `counts_unsupported_files_seen_during_a_folder_walk_as_skipped`), `cargo clippy --lib` clean (same 4 pre-existing unrelated warnings noted below), `tsc --noEmit`/`eslint` clean across `tahti-web`/`player`/`storybook`, full `tahti-web` vitest suite 569/569 (fixed one pre-existing test-order-dependent flake in `DesktopLibraryPanel.native.test.tsx` found along the way — it never set `__TAHTI_NATIVE_CAPABILITIES__` itself and only passed when an earlier-run file happened to leave it truthy). Not live-browser-verified (no display/GUI in this sandbox); bindings.ts regenerated via the headless `export_bindings` binary and diffed by hand.
- [ ] Extract title, artists, album artist, album, track/disc numbers, year/date, genre, comment, duration, codec/container, bitrate, sample rate, channels and embedded artwork. **2026-09-21 mostly done, embedded artwork still open:** migration `0004_full_metadata.sql` adds `album_artist`, `track_no`, `disc_no`, `year`, `genre`, `comment` (capped at 500 chars) and `bitrate_kbps` (derived from size and duration, so it is the average, not a tag); `metadata.rs` fills them from symphonia's standard tag keys (track/disc parse `3/12`, year parses `2019-05-01`, an original-date tag only fills in when there is no plain date). Unknown values stay empty/NULL, never guessed from the filename (only the title falls back to it, as before). Search index rebuilt to also cover album artist, genre and comment. Existing rows get the new fields on their next import or rescan of the folder (rescan only re-reads files new to a root, so re-import to backfill). Tested with RIFF INFO fixtures (RIFF maps track number to `IPRT`, not `ITRK`); the FLAC/Vorbis path uses the same tag mapping but has no fixture of its own, since no FLAC encoder is available here. Panel shows year, genre and bitrate. Left open: embedded artwork (needs the Phase 2 artwork cache), ID3/MP4 containers (the importer is FLAC/WAV only), ReplayGain/BPM/key tags (Phase 5). Preserve unknown values; filename is a fallback, not a guessed artist. - **2026-09-17 (second pass):** untouched. This is almost entirely `metadata.rs`/symphonia Rust work and no toolchain was available to write or verify it (see note above) - writing unverified tag-extraction code with no way to even `cargo check` it was judged too risky to ship blind, consistent with this doc's own standard of not claiming machine-verified work that wasn't. Left fully open for a session with a working toolchain.
- [ ] Index in bounded batches with background concurrency limits; commit incrementally and resume interrupted jobs without duplicate rows. Validate decodability independently of accepted extensions.
- [x] Resolve durable native file locations into the shared player at playback time. Verify seek, duration, next/previous, pause/resume and queue identity for local tracks. Do not persist blob URLs or send local IDs to sound analytics/cloud collection endpoints. - **2026-09-17:** No Rust toolchain available this session (`cargo`/`rustc` not on PATH, unlike the 2026-09-16 pass — environment reset between sessions), so this slice is frontend-only; native-side work (folder import, richer tag extraction, batch/resumable jobs, re-scan/relink) is untouched, left for the remaining unchecked items below. Found and fixed two real gaps: (1) native library tracks had a Play button but no way to add to the queue at all — `next`/`previous`/multi-track playback was structurally unreachable for the native catalog. Added a "Queue" button in `DesktopLibraryPanel.tsx` mirroring the browser-fallback local list, wired through the existing `nativeLibrary.resolve()` + `playerStore.enqueue()`. (2) `libraryStore`'s persisted play history stored each entry's raw `streamUrl` verbatim — for local tracks that's a `blob:` object URL (browser File-API fallback, revoked on reload) or a Tauri `asset://` URL (scoped to the app session, not guaranteed valid after restart); "Play now" from History or the "Continue listening" card would silently hand the audio element a dead URL after a reload/restart. Added `redactEphemeralStreamUrl` so only the *persisted* copy drops a local track's `streamUrl` (in-session replay is unaffected); added `resolveLocalPlayableForReplay` (`lib/nativeLibrary.ts`) to re-resolve a redacted local entry through the native catalog by id before replay, used by both `HistoryListSection` and `ListenView`'s "Continue listening" card, surfacing a "re-import" toast when there's no native catalog to resolve from (browser-fallback tracks have no durable handle to recover). Verified by code inspection that `AudioEngine.tsx` already treats native/blob local URLs as a plain `<audio src>` (protocol `'https'`, no `.m3u8`), so seek/duration/pause/resume/next/previous go through the same generic path as every other track once a real queue exists — not live-tested against the actual Tauri app (no display/GUI in this sandbox). Confirmed no client-side call sends a `local:`-prefixed id or a local `streamUrl` to any server endpoint (`/api/me/stats/plays*` are read-only stats fetches, not play-reporting). Added tests: `stores/libraryStore.test.ts` (redaction happens only for persisted local entries, in-memory copy keeps its URL, non-local entries unaffected), `lib/nativeLibrary.test.ts` (`resolveLocalPlayableForReplay`'s four cases). Full `@tahti-player/tahti-web` vitest suite: 533/533 (the 13 "failures" are pre-existing Playwright e2e specs vitest isn't configured to run, same known issue noted elsewhere in this doc). `tsc --noEmit`/`eslint` clean on all touched files.
- [x] Re-scan roots idempotently; mark missing/unavailable files; provide relink file/folder and reveal-in-folder. Preserve IDs during proven relocations; ask for resolution when identity is ambiguous. - **2026-09-18 partial:** wired `list_unavailable()` through a generated Tauri binding and the native adapter, added an idempotent check of already-missing catalog entries, and added Local files UI to check missing files or locate a replacement file. File relink preserves the catalog ID, refreshes extracted metadata, clears unavailable state, rejects unknown IDs, and reports a replacement path already owned by another catalog row instead of guessing identity. A new headless `export_bindings` binary removes the former GUI-only binding-generation blocker. Rust coverage now includes recovered/still-missing re-scan behavior, idempotency, metadata refresh, ID preservation, unknown IDs and duplicate paths; the web UI has a rendered-state test and Storybook state. Root walking/new-file discovery, folder relink and reveal-in-folder remain open. **2026-09-21: reveal-in-folder shipped.** New `library_reveal` command resolves a track's path then calls `app.opener().reveal_item_in_dir(path)` — the opener plugin's Rust API directly, not the IPC-scoped IPC command, since the frontend-facing `opener:allow-reveal-item-in-dir` permission in `capabilities/default.json` is intentionally scoped to `$APPLOG` for the existing log-folder feature and import locations are arbitrary; calling the plugin's Rust function bypasses that ACL the same way `library_resolve` already bypasses the asset-protocol scope with a per-path `allow_file`, since the user granted access to this exact file by importing it. Wired into `DesktopLibraryPanel` as a "Reveal in folder" icon button next to Play/Queue for available tracks. **Root walking/new-file discovery and folder relink remain open** — both need the not-yet-built "library roots" concept from this doc's own Product/architecture section (a root is currently implicit per-import, not a persisted, re-scannable entity), which is real design work, not a quick follow-on to this pass. **2026-09-21 (second pass): library roots shipped, item closed.** Migration `0003_roots_and_search.sql` adds `library_roots` (unique path, `last_scanned_at`) and a nullable `library_tracks.root_id` (ON DELETE SET NULL: removing a root only stops tracking it, tracks stay). New commands `library_list_roots` / `library_add_root` (picker → register → initial scan) / `library_remove_root` / `library_rescan_roots` / `library_relink_root`, all pool-level functions tested. Rescan walks each root and imports only files not yet members of that root (ad hoc imports under the folder are re-read once and adopted, never duplicated), then refreshes missing/recovered state for every track in the root in one blocking pass + one transaction (so files that vanish are caught too, not just previously-missing ones); a second scan with no changes changes nothing. Folder relink re-points every track whose same relative path exists in the new folder **with the same byte size** (proven relocation, IDs preserved), leaves unproven tracks untouched, rejects a folder matching nothing or already being another root. UI: a "Watched folders" block in `DesktopLibraryPanel` (add / rescan / relink / stop-watching behind a confirm dialog, spinners while running, toasts for every result). Still open from this item's wider wording: filesystem *watching* (debounced events, Phase 6) — rescan is manual for now; re-extraction when a known file's size/mtime changes (Phase 6).

**Performance pass (same date), "lightning fast" local files:** (1) search moved from a four-column `LIKE '%x%'` full scan to an FTS5 trigram index (same case-insensitive substring semantics, terms AND across columns, triggers keep it in sync; terms under 3 chars fall back to LIKE); empty search no longer evaluates LIKEs at all and pages off the title index. Measured on the 100k-row fixture in a debug build: first page 1.3 ms, indexed search 1.9 ms (asserted in `paginates_100k_generated_rows`, run with `--ignored`). (2) Import reads tags concurrently in batches of 16 and commits each batch in one transaction (was one autocommit per file); progress/cancel granularity is now per batch. (3) SQLite tuned for both native DBs: `synchronous=NORMAL` (safe under WAL), 32 MB page cache, in-memory temp store, 256 MB mmap. (4) Frontend: `withReadCache` (nativeLibrary.ts) wraps `list`/`listUnavailable`/`listRoots` in an LRU (60 pages) with shared in-flight requests, cleared by every mutating call even when it fails; `resolve` deliberately uncached since it doubles as the existence check. Search input is debounced (120 ms), and stale responses are discarded via a request counter. Not measured: real 100k *import* timing (needs real audio fixtures) and release-build numbers (Phase 0 baseline item).
- [x] Migrate old filename-only entries as unresolved references. Do not silently bind them to a different same-named file. Keep removal from catalog separate from physical file deletion. - **2026-09-17 (second pass):** found and fixed the real bug this item describes, scoped to the browser-fallback catalog (`stores/localLibraryStore.ts`) - the native SQLite catalog was never at risk here since `path` has always been its identity key (`ON CONFLICT(path)` in `local_library/mod.rs`). `addFiles` previously restored *any* unresolved entry whose `fileName` matched a newly chosen file, regardless of size - two different files that happen to share a name (including rows persisted before `fileSize` was ever tracked, which come back as `fileSize: undefined`) would silently take over the old entry's id, corrupting its identity. Added `isSameUnresolvedFile` (name **and** size must match; `lastModified` must also match when both sides record one) and use it in place of the name-only check - a same-named file that doesn't match is now added as its own new track, leaving the old reference visibly unresolved ("Re-import ... to play") until the user relinks or removes it, never guessed at. Verified by reading the code (not assumed) that "removal from catalog separate from physical file deletion" was already true on both paths: browser `remove()` only clears the persisted entry and revokes its blob URL; native `library_remove` only runs `DELETE FROM library_tracks` - neither touches the file on disk. Added tests in `localLibraryStore.test.ts`: name+size match still restores identity, a same-size name-only match on a legacy no-size row does not, a name-only match with a different recorded size does not, plus a dedicated `isSameUnresolvedFile` unit-test matrix (match, size mismatch, missing size, already-playable, `lastModified` mismatch). Does not touch the native catalog (nothing to fix there) or add a relink UI (that's the item above, blocked on the same missing toolchain).

- [ ] Import an iTunes / Music.app library XML (`iTunes Music Library.xml`, plist) together with its metadata. Parse tracks (title, artist, album artist, album, genre, year, track/disc numbers, comments, BPM, date added), user data (rating, play count, last played, skip count, loved) and playlists (incl. folders and ordering) without touching the audio files. Resolve each track's `file://` Location to a local path (decode percent-escapes, map old drive/root prefixes via a user-confirmed remap) and link it to the catalog by path, importing missing files through the normal importer; report unresolved/missing/unsupported-format tracks instead of dropping them silently. XML values are imported as user metadata layered over extracted tags (never overwriting them) and feed Phase 4's ratings/play counts/history and Phase 3's playlists, so the playlist/rating parts land with those phases; the track-linking and metadata-overlay parts belong here. Needs a streaming plist parser (libraries can be 100MB+), preview-before-commit with counts, idempotent re-import, and fixtures with Unicode paths, duplicate tracks, and missing files. Non-FLAC/WAV entries (MP3/AAC) stay unresolved until the importer supports those formats.

**Exit demo:** import a 1,000-file nested folder, restart offline, search a basic title list and play/seek tracks without re-import. Re-scan adds no duplicates; corrupt files do not abort the batch; disconnected files remain visible and can be relinked.

## Phase 2 — Catalog, sorting, filtering and search

Depends on: phase 1. Main Library workspace, not a tiny sidebar list.

- [x] Build a virtualized multi-select TrackTable with configurable/reorderable columns and persisted widths, sorting and view state. **2026-09-21 native catalog (second half):** new generic `CatalogTable` in `@tahti-player/ui` (with Storybook story) drives the native Local files list: virtualized rows, paged on scroll (100 at a time, next page requested near the end), server-side sort, multi-select with shift-range and select-all-loaded, resizable columns, and the settings dialog above. Sorting runs in SQLite: click a header for ascending, again for descending, again for the default title order; blank values sort last both ways; ties always break on title then id so paging never repeats or skips a row (tested across 450 rows with heavy ties for every tie-heavy column, both directions). Removal (single or bulk) now asks for confirmation. Measured on the 100k-row fixture in a debug build (SQLite compiled unoptimized there): title sort ~3 ms per page, other columns 40-150 ms per page, worst are artist (~150 ms) and album (~125 ms) because their blank-last ordering cannot use an index; expected several times faster in release, and worth an index or a stored sort key if it still feels slow on a real library. Not done: select-all across pages (next item in this phase), keyboard row navigation, drag-reorder of columns on the header itself. Reuse Storybook primitives and preserve player/nav chrome. — **2026-09-18 (browser-fallback path only):** `DesktopLibraryPanel.tsx`'s browser-fallback list (playable tracks) now renders through `PlayableTrackTable` (real `TrackTable`, already virtualized, sortable, filterable, `selectable` with a bulk-action toolbar) instead of a hand-rolled `<ul>` — the exact reuse this doc's 2026-09-12 section called out as ready ("reusable once Phase 2 wires `TrackTable` to the native catalog; it doesn't care where `tracks` comes from"). Unresolved (needs-reimport) tracks are kept in a small separate list above the table rather than mixed in — `TrackTable`'s row actions have no per-row "disabled/needs action" concept, so folding a broken reference into the table would either hide it or show a Play button that silently does nothing; a distinct always-visible list preserves today's explicit "Re-import X to play" messaging instead. **Native-catalog tracks intentionally NOT wired to `PlayableTrackTable` this pass** — `playableFromNativeTrack` needs an async per-track `nativeLibrary.resolve(id)` call for its `streamUrl` (paged native list, by design — "IPC returns typed pages/progress, not a whole decoded collection"), but `PlayableTrackTable`/`TrackTable` need a fully-materialized `TahtiPlayable[]` synchronously; resolving every visible native track's URL upfront just to populate the table would mean N native IPC calls per page render, working against the doc's own paging design. Left on its existing lazy-resolve-per-click list. A real fix needs either an async-resolve hook added to `TrackTable`'s play/queue actions (touches a heavily-reused shared component) or a native `resolve`-batch command — flagging rather than forcing either without a decision. Verified: `tsc --noEmit` / `eslint` clean, `DesktopLibraryPanel.test.ts` (3/3) still passes, `DesktopLibraryPanel.stories.tsx`'s `SearchableLibrary` story already covers one playable + one unresolved track (exercises both new code paths). Not live-browser-verified.
- [x] Add Tracks, Artists, Albums, Genres and Folders browsing backed by the same catalog; show track count, duration and local file size totals separately from cloud storage. - **2026-09-21:** migration `0005_browse_indexes.sql` adds a stored `folder` column (directory of the path, trailing separator kept, backfilled in SQL and maintained on import, relink and root relink) plus indexes on folder, genre, album and the artist grouping key (album artist, falling back to artist, so compilations stay together). New commands `library_facets(kind)` (groups with track count, total duration and total size), `library_totals()` and a `filter` argument on `library_list`; every filter is a bound parameter and combines with search. Grouping ignores case; an album is (name, album artist), so two albums that share a name stay separate; missing values form an explicit group ("Unknown artist", "No genre", "Loose files") instead of disappearing. UI: Tracks/Artists/Albums/Genres/Folders tabs in `DesktopLibraryPanel`, a group list with counts/duration/size, click a group to see its tracks under a removable filter chip, and an "N tracks · 3 h 20 m · 3.5 GB on this device" line. The frontend cache also covers facets and totals. Measured on the 100k-row fixture (debug build): artists 77 ms, albums 229 ms, genres 23 ms, folders 21 ms; a filtered page under 1 ms. Group lists are not paged or virtualized, so a library with a very large number of distinct albums renders them all at once (fine at 1,000; revisit when the TrackTable work in this phase lands). Still open in this phase: virtualized/sortable table, facet ranges (year/duration/format/bitrate), keyboard selection, inspector.
- [x] Table settings modal: the user chooses which fields the track table shows, and in what order. - **2026-09-21:** gear button in the table toolbar opens a "Table settings" dialog (`CatalogTableSettingsDialog`) with a switch per field (Title is required and always shown), move up/down per field, and "Reset to defaults". Twelve fields are available: title, artist, album, album artist, year, genre, track #, time, format, bitrate, size, date added; album artist, track #, bitrate and date added are off by default. Choices, order, widths and the sort are remembered on this device (`usePersistedCatalogTable`) and re-validated against the current columns on load, so a later version adding or removing a field never breaks a saved view. Open follow-ups: drag-to-reorder inside the dialog (buttons only for now), a comment column, saved named column layouts (that is the "saved column layouts" item below), and syncing the layout to the user's account.
- [x] Add indexed text search over title/artist/album/genre/path/comment; combine search with facets and ranges for year, duration, format, bitrate, root, date added and availability. Add BPM/key/analysis facets when phase 5 lands. - **2026-09-21 done.** Search was indexed earlier (FTS5 trigram over title, artist, album artist, album, genre, comment and path). Filters added: `TrackFilters` (year from/to, length min/max, minimum bitrate, format list, watched folder, added-since date, available/missing) combine with search and the browse group in one WHERE built from bound parameters (`ListQuery`, shared by paging, select-all and play-all so "matching" always means the same thing). A "Filters" dialog (`LocalLibraryFilters`) drafts edits and applies them together; the button shows how many restrictions are active. `library_filter_options` supplies the formats and year span. Left for BPM/key/analysis facets when Phase 5 lands (as the item says).
- [x] Execute sort/filter/search in SQLite with stable tie-breaking and paged results. Cancel or discard stale queries. Do not load all rows into Zustand for client-side filtering. - **2026-09-21 done.** Sort, filter and search all run in SQLite (100 rows per page, stable title/id tie-break, album sort orders disc then track). Nothing is filtered client-side and no library rows live in Zustand. Stale queries are discarded rather than cancelled: every request carries a counter and a response that is no longer the latest is dropped (tested with a slow outdated answer arriving after a newer one); the queries themselves take milliseconds so there is nothing worth aborting in SQLite.
- [x] Support shift/range selection, select all matching results across pages, keyboard actions, play selected, play filtered results, play next and enqueue. Define snapshot ordering so queue insertion is predictable when a filter changes. - **2026-09-21 mostly done; keyboard actions still open.** Shift-click range selection and select-all-loaded were already in `CatalogTable`. New: once every loaded row is selected and more match, the table offers "Select all N"; that asks the catalog for every matching id in exactly the paging order (`library_matching_ids`, same WHERE and ORDER BY as `library_list`, tested equal to paged order for four sort/search/filter combinations) and the count line then reads "All N selected". Selection actions in the table toolbar: **Play** (replaces the queue, starts the first), **Play next** (inserts right after the current track, in order, moving any already queued), **Add to queue** (appends, skipping tracks already queued) and **Remove** (confirmation first, one transaction via `library_remove_many`, files on disk untouched). **Snapshot ordering rule:** a selection is a set of ids, and every action uses the order the table shows *at the moment you act* (current sort, search and group), not the order you clicked; so a re-sort before queueing changes the queue order predictably. A selection belongs to the search/group it was made in and is cleared when either changes (a re-sort keeps it). Playback prep (`library_prepare_playback`) loads rows for 500 ids at a time, keeps the requested order, skips and reports files that are missing (and persists them as unavailable) and grants asset access per file; play/queue actions take at most the first 5,000 selected tracks and say so when they cut. Bulk queueing is one store update and one toast (`enqueueMany` / `playNextMany`) instead of one per track. Also fixed while here: the table could re-request a missing page forever when a later page came back empty; it now settles on what it has. Open: keyboard navigation and shortcuts for rows (space to select, arrows, Enter to play), "play filtered results" without selecting first, and a cap warning before removing an entire library. - **2026-09-21 done (keyboard and play-all added).** Keyboard on the table (it is a focusable grid): arrows, Home/End, PageUp/PageDown move the active row; Shift+arrows extend the selection; Space toggles; Ctrl/Cmd+A selects everything that matches (all pages); Escape clears; Enter or double-click plays the row; `I` opens its details. "Play all" plays every matching track (same order, same 5,000 cap) without selecting first.
- [x] Add a details inspector, empty/loading/error states and saved column layouts; retain position and filters when returning from a detail view. - **2026-09-21 done.** Inspector: a details dialog per track (all tags, path, sample rate, channels, bit depth, bitrate, size, added date, availability) with play, queue, reveal, locate/relink and remove; opened from a row button or the `I` key. States: loading spinner while a query runs, an error state with Retry, an explicit "No tracks match" state with a one-click way to clear search and filters (previously an empty result looked like an empty library), and "Loading…". Saved layouts: name and save the current columns, order, widths and sort from the table settings dialog, apply one later, delete one (with confirmation); stored on this device next to the current view. Retention: search text, browse tab, group, filters, scroll position and how many rows had been loaded are kept in memory for the session (mirrored to sessionStorage), so leaving to another page and coming back lands in the same place; on return the rows scrolled past are preloaded before the position is restored (capped at 2,000), and changing search, group or filters resets it. Not persisted across app restarts by design.

**Exit demo:** in a 10k-track fixture, combine artist + format + year filters, sort by album/disc/track, select all matches across pages and queue them in displayed order. Verify bounded mounted rows, query timings and UI responsiveness against phase 0 budgets; stress 100k catalog rows.

**Phase 2 status (2026-09-21): complete.** Exit demo is covered by the automated `exit_demo_filter_sort_select_all_in_displayed_order` test on a 10k fixture (genre + format + year filters, sort by album/disc/track, select-all ids equal the paged order, playback batch prepared), the 100k timing test (first page ~2 ms, search ~2 ms, facets 20-230 ms, sorts 3-150 ms per page in a debug build), and bounded mounted rows (only visible rows plus overscan render). Not done and worth a real-hardware pass: release-build timings against the Phase 0 budget (never recorded, see Phase 0), 100k-row filter timings, and a real 100k-file import.

## Phase 3 — Durable local playlists

Depends on: phase 2. Deliberately before analysis or Soulseek.

- [x] Create, rename, duplicate and delete local playlists; add tracks from selection, filtered results, albums, folders and queue. Persist ordering and repeated entries using entry IDs distinct from track IDs. - **2026-09-21 done.** Migration `0006_playlists.sql`: `library_playlists` (unique name, case-insensitive) and `library_playlist_entries` (own entry id, dense `position`, `track_id` nullable with ON DELETE SET NULL, plus snapshot path/title/artist/duration). Because the entry, not the track, carries the order, the same track can appear twice and order survives track relinking (root relink keeps track ids). Removing a track from the library never deletes its playlist entries: they stay, shown as Unavailable, and are re-linked by path when the file is imported again (checked each time a playlist is opened). Add from: selected tracks, all results of the current search/filters ("Add all to playlist"), an album, artist, genre or folder group (album/disc/track order), and the queue. Additions append; the same track can be added again. New "Playlists" tab in Local files (list with counts, duration, unavailable count; play, shuffle, duplicate as "<name> copy", rename, delete with confirmation). Limitation: the player queue is keyed by track id, so playing a playlist with a repeated track queues that track once; the repeat is kept in the playlist itself.
- [x] Support drag/keyboard reorder, multi-remove, undo for playlist edits, play/shuffle playlist and save queue as a local playlist. Handle unavailable tracks visibly without deleting their entries. - **2026-09-21 done.** `CatalogTable` gained reordering: drag a row (or the whole selection) with a drop line showing where it lands, or Alt+ArrowUp/Down on the active row or selection; the move is one native call that counts the target index among the entries that stay. Multi-remove (selection, Delete key, or the row button) returns the removed entries so Undo restores them with their original ids and positions; moves undo by restoring the previous full order; Undo button and Ctrl/Cmd+Z, 20 steps, plus an Undo action on the toast. "Play" starts the playlist from the top, "Play from here" (or Enter/double-click on an entry) starts at that entry and wraps, Shuffle plays a shuffled copy. Unavailable entries are muted and skipped when playing, never deleted. Saving the queue: a new "Save queue as local playlist" button in the queue panel saves the local tracks in queue order and states how many streamed tracks (Tahti, radio, live) cannot be stored locally.
- [x] Provide local save independently of cloud Save as playlist. Keep existing cloud collection behavior explicit; do not silently omit local tracks when the user intends a local playlist. - **2026-09-21 done.** Local playlists use their own tables and commands; nothing touches cloud collections. On the desktop app the queue panel now has two clearly labelled buttons, "Save queue to cloud playlist" (unchanged behavior) and "Save queue as local playlist". The cloud dialog no longer lumps local files in with "external tracks": it says how many local files live on this device and can't go to a cloud playlist, and points to the local option.
- [x] Import/export M3U/M3U8 with UTF-8 and relative/absolute paths; preview unresolved entries and support relinking. Round-trip ordering and duplicates. Explain portability for files outside the export root. - **2026-09-21 done.** Export (playlist row or open playlist -> Export) writes UTF-8 `#EXTM3U` with `#PLAYLIST` and `#EXTINF` (artist - title, seconds) and one path per entry in order, repeats included; entries whose file is missing keep their last known path. Path style is chosen up front: *relative to the saved file* (files inside its folder are plain relative paths; files outside use `../`; files on another drive fall back to full paths) or *full paths*. The dialog explains portability before saving and the result toast reports exactly how many files were outside the folder or fell back to full paths. Import reads UTF-8 (BOM allowed) and falls back to Latin-1 for old `.m3u`; handles `\r\n`, comments, blank lines, `file://` URLs with percent escapes, Windows-style paths opened elsewhere, and relative paths resolved against the list's folder. A preview dialog runs before anything is created: it counts entries already in the library, files that exist but are not imported yet, files not found, unsupported formats (only FLAC/WAV import today) and stream URLs, lists the first 200 unresolved entries with the reason, offers a switch to also add the not-yet-imported files to the library, and "Find missing files in a folder…", which matches by folder path (dropping leading folders one at a time) and then by a unique file name. Nothing is dropped: every line becomes an entry in file order; unresolved ones are kept as Unavailable entries that link up by path when the file joins the library, or by hand with the new "Locate the file" row action (imports the file if needed). A taken playlist name fails before any file is imported. Round trip export -> import preserves order and repeats (tested).
- [x] Persist queue references/order and restore paused after restart; resolve sources lazily and avoid autoplay on launch. - **2026-09-21 done.** The queue (and which track is current) is saved to localStorage a half second after it changes and restored at startup. Saved: Tahti and local tracks, in order; local files by library id only, never a session URL; Tahti tracks with their stream URL. Skipped: radio and live entries (they resume from their own state). On launch the queue comes back **paused with nothing loaded**: the audio engine leaves a restored current track alone until you press play or pick another track (`restoredPending`, covered by tests against the real `AudioEngine`, including that ordinary playback still autoplays). Local sources are resolved lazily after startup: the current track first (one call), the rest in batches of 500 that yield between calls; files that are gone are removed with a single notice, and a library that cannot be reached leaves the rest untouched. Without the native library (browser) local files are not restored. Limits: the position within the track is not restored (it restarts at 0:00), and a saved Tahti stream URL that has since expired will fail to play like any stale link.

**Exit demo:** create a 50-track playlist from filtered results, reorder it, save the queue, restart offline and play both. Export/import round-trip preserves order and duplicates; moving a root and relinking preserves membership.

**Phase 3 status (2026-09-21): complete.** The exit demo is covered by tests: `exit_demo_fifty_tracks_from_filtered_results_reordered` (50 tracks from filtered results, reordered), the queue restore tests (saved queue comes back paused, local sources resolved lazily), `export_then_import_round_trips_order_and_repeats`, and `relinking_a_track_keeps_playlist_membership` plus root relink keeping track ids (membership survives a moved root). Not verified in the real app (no display here): a real restart with a saved queue, and export/import through the native file dialogs.

## Phase 4 — Catalog editing and organization

Depends on: phase 3.

- [x] Add single/bulk metadata editing with mixed-value indicators, preview of affected tracks, undo and field-level provenance. Store user overrides separately from extracted tags; rescanning must not erase edits. - **2026-09-21 done.** Migration `0007_catalog_editing.sql` + `local_library/catalog.rs`. Nine fields are editable (title, artist, album artist, album, genre, year, track #, disc #, comment). The edited value lives in the `library_tracks` column (so search, sort and indexes are unchanged) and `library_track_overrides` records `(track, field, value, extracted)`: which fields are hand-set and what the file's own tag says. A re-import or rescan calls `reapply_overrides` inside the same transaction: edits are put back, `extracted` is refreshed to the file's new tag, and an edit that now equals the file's tag stops being an override. "Revert to file tag" goes to the *latest* file tag. Bulk edit: `library_field_summary` gives the mixed-value indicator (distinct count) and how many tracks hold hand edits per field; `library_edit_preview` runs the real edit inside a transaction and rolls it back, so the preview shows exactly what would change (tracks changed vs already matching, first examples, and validation errors such as a non-numeric year) before Apply; `library_edit_tracks` returns per-field snapshots, `library_restore_edits` restores them exactly (value and override state), surfaced as an Undo action on the result toast. Dialog: `TrackEditorDialog` (toolbar "Edit tags", row button, `E` key). Provenance is shown in the editor as a per-field Revert control with the count of hand-edited tracks; `library_provenance` (value, edited, file value, edited-at) is exposed but the read-only inspector does not show it yet. Files on disk are never modified.
- **Verified:** `catalog_tests.rs` — bulk edit + exact undo, rescan keeps edits and tracks the new file tag, edit equal to the file tag drops the override, preview writes nothing, invalid edits rejected before any write, mixed-value summary, search index follows edits, and the exit-demo sequence (bulk-correct, undo, re-correct, rescan keeps it).
- [x] Add ratings, color labels/custom tags, comments, play count and last played; record local listening history locally. - **2026-09-21 done.** Columns `rating` (0-5), `color` (fixed set of seven), `play_count`, `last_played_at`; tags in `library_tags` / `library_track_tags` (case-insensitive unique names, a tag nobody carries disappears). `TrackOrganizeDialog` (toolbar "Rate and label"): stars, color swatches, tag chips with add/remove and suggestions from existing tags; each change applies at once and offers Undo (`UserDataSnapshot` restore). Rating, label, plays and last played are table columns (rating on by default, the rest via table settings), rating/plays/last-played are sortable in SQLite, and the Filters dialog gained minimum rating, color and tag. Comments were already editable above. Play counting (`lib/playCounting.ts`): a local track counts once per play-through after 30 s or half its length, whichever is less; skipping counts nothing, seeking back to the start of the same track re-arms it; stored on this device only, nothing is sent to a server. Not done: a separate listening-history *timeline* for local plays (only count and last-played; the cloud History view is unchanged).
- [x] Add duplicate review using exact content hashes on demand, then metadata similarity as a separate suggested match. Never auto-delete similar tracks. - **2026-09-21 done.** "Find duplicates" in the new Library tools block opens `DuplicatesDialog`. "Compare file contents" SHA-256-hashes every available file on demand (one at a time, off the async runtime, progress event + Stop button; size and mtime are stored with the hash so unchanged files are skipped next time and changed ones are re-hashed). Groups: **Identical files** (same hash), **Same name, different files** (same title/artist/length but every file hashed and the contents differ: shown as confirmed non-duplicates) and **Possibly the same track** (same title and artist after folding case/punctuation, lengths within 2 s, not yet compared). An identical set is never repeated as a suggestion. Review only: each row can be revealed or removed from the library behind a confirm dialog, which never touches the file; nothing is removed automatically. Not done: keeping/merging metadata from the removed copy into the kept one, and "ignore this group".
- [x] Add explicit optional write-tags-to-files with supported-format matrix, read-only/error handling and recoverable writes; preserve original files by default. - **2026-09-21 done.** `local_library/tag_writer.rs` using the `lofty` crate. Toolbar "Write tags to files" on a selection opens `WriteTagsDialog`: a preview (files that will be written, tracks with no edits, skipped files with the reason, supported formats) and a "keep a backup copy" switch (on by default). Only hand-edited fields of the selected tracks are written; tracks without edits are never touched. **Format matrix:** FLAC (Vorbis comments) writes all nine editable fields; WAV (RIFF INFO, plus any ID3v2 chunk already in the file so the two never disagree) writes everything except album artist and disc number, which INFO has no slot for: those are detected on read-back, counted as "no place in the file format" and stay in the library only. Other formats can't be imported yet, so there is nothing else to write. **Recoverable:** the file is copied to `<name>.tahti-tmp.<ext>` beside it, tagged there, re-read to verify (parses, same duration and sample rate, decodes, every written value reads back), the original is copied to `<name>.<ext>.tahti-backup` (numbered if one exists), then the temp file is renamed over the original; any failure removes the temp file and leaves the original byte-for-byte untouched (tested with a corrupt file). Read-only and missing files are skipped with a reason. After a write the track's size is refreshed and its content hash cleared; an edit stops being an override only once the app's own reader sees it in the file, otherwise it stays as an override so the library keeps showing it. Importing now also falls back to `lofty` for tags symphonia does not surface (its WAV reader stops at the audio data, so INFO/ID3 stored after it, where writers put them, were invisible); it only fills empty values, so anything symphonia found wins. A real FLAC fixture (`local_library/fixtures/tone.flac`, 12 KB, made with `flac`) now exists, which also gives the Vorbis-comment import path its first real test that the Phase 1 notes said was missing. Tests in `tag_writer_tests.rs`: WAV write is recoverable and only touches edited files, unsupported/read-only/missing skipped with reasons, a failed write leaves the original untouched, FLAC import and round trip (all seven tested fields visible to a fresh catalog with no overrides left).
- [x] Add catalog backup/restore including playlists, overrides, roots and analysis references. Preview root mappings and distinguish database backup from audio-file backup. - **2026-09-21 done (analysis references: none exist yet, Phase 5 adds them to the format).** `local_library/backup.rs`. "Back up catalog" writes one portable JSON file (`.tahti-backup`, written to a temp file then renamed): watched folders, every catalog file path, hand edits, ratings, labels, tags, play counts and playlists with their entries. It is a catalog backup, not an audio backup, and says so in the dialog and the toast; extracted tags are not stored (a restore re-reads them from the files). "Restore…" picks a file, then `BackupRestoreDialog` previews before anything is written: backup date, track/playlist/edit counts, how many files exist at their (mapped) location, each watched folder with its mapped destination and whether it exists, a "Choose new folder…" button per folder that re-runs the preview, and examples of files not found. Mapping is old folder -> new folder (longest match wins, folder boundaries respected, case-insensitive on the ASCII part, Windows-style paths mapped onto Unix ones, no panics on multi-byte paths). Restore imports every found file (grouped under its restored watched folder), applies user data and edits on top of the fresh tags (edits stay real overrides, so they survive a rescan), and recreates playlists (a taken name becomes "<name> (restored)", never an overwrite; entries whose file is missing are kept as Unavailable and link up later). Files not found are reported and skipped; their ratings/edits are not restored. A file that is not a backup, or is from a newer version, is rejected with a clear message. Tests: `backup_tests.rs` (fresh-profile restore with a moved root incl. edits, ratings, color, plays, tags, playlists, root membership and a rescan surviving; missing files reported; name collision; junk file; path mapping).

**Exit demo:** bulk-correct an album, undo, re-scan without losing overrides, review same-name nonduplicates correctly, and restore catalog/playlists into a fresh profile with remapped roots.

**Phase 4 status (2026-09-21): complete.** The exit demo is covered by tests: `edits_are_searchable_and_bulk_correct_then_undo_then_rescan_exit_demo` (bulk-correct an album, undo, re-correct, re-scan keeps the edit), `duplicates_exact_by_hash_similar_by_metadata_never_confused` (same-name different-content files reported as non-duplicates, identical copies as exact, nothing removed), and `backup_restores_into_a_fresh_profile_with_remapped_roots` (catalog, edits, ratings, tags and playlists restored into an empty catalog with the music folder moved). Frontend: `CatalogDialogs.test.tsx`, `playCounting.test.ts`; full `tahti-web` suite 673/673 (the e2e specs are Playwright, not vitest), `cargo test --lib local_library` 93/93, clippy clean for the new code. Also added in the follow-up pass: write-tags-to-files (above); **merge on duplicate removal** ("Keep this" on a duplicate row, behind a confirm: best rating, first color, all tags, summed plays, latest last-played, playlist entries and history repointed at the kept track, the other copies removed from the catalog, files untouched; `merge_tracks`, tested); **listening history** (migration `0008_play_log.sql`, one row per counted listen with a title/artist snapshot that survives removing the track, `PlayHistoryDialog` from the Library tools "History" button, newest first, paged, clearable behind a confirm; clearing keeps per-track play counts); **inspector provenance** (edited tags show "Edited · file says …"). Hashing throughput measured with the ignored `hashing_throughput_on_large_files` test: 1 GiB across four files in 14.8 s = about 69 MiB/s in a *debug* build (unoptimised SHA-256); release is expected to be several times faster, not measured. Not verified in the real app (no display here): the native file dialogs for backup/restore, the folder picker, and the look of the dialogs. Open follow-ups: analysis references in the backup once Phase 5 adds them, the play log in the backup file, an "ignore this group" for duplicates.

## Phase 5 — Audio analysis and smart playlists

Depends on: phases 1–4. Analysis enriches the catalog without blocking playback.

- [x] Implement background jobs for waveform peaks, loudness/true peak, BPM and musical key. Read existing BPM/key tags first; store tag values, computed estimates and user corrections separately. - **2026-09-21 done.** Migration `0009_analysis.sql`: `library_analysis` (tag values, estimates with confidence, loudness, true peak, 400-bucket peaks, algorithm version, analyzed file size/mtime) and `library_analysis_user` (corrections). The value filters, sorts and smart playlists use (user > tag > estimate) is copied onto `library_tracks` (`bpm`, `musical_key`, `loudness_lufs`, `analyzed`) by `analysis::refresh_effective`, so those stay plain indexed SQL. Tag BPM/key are read with lofty when a file is analyzed (not at import), key spellings (Am, A minor, Bbm, Camelot 8A) normalized. DSP in `analysis_dsp.rs`, no new crates.
- [x] Select and validate native analysis implementation on reference fixtures; record algorithm/version, units, confidence where available and analyzed file identity. Label BPM/key as estimates and allow correction/re-analysis. - **2026-09-21 done, with a caveat on fixtures.** Validated on synthetic signals only: BS.1770 sine calibration (within 0.1 LU), a silent file, below-gate audio, steady kick tracks at 100/120/128/140 BPM (within 2 BPM), chord fixtures for key (Am, G), an inter-sample-peak sine. **Not validated against real music or a reference analyzer**, so the published accuracy limits (module header of `analysis_dsp.rs`) are design expectations, not measurements; do a pass with a small labelled real-music set before trusting the numbers. Inspector labels BPM/key as estimates with a confidence, offers "Your BPM/Your key" corrections (with undo) that re-analysis never overwrites, and Re-analyze.
- [x] Decode incrementally where supported, cap worker count/memory, prioritize playback, and provide pause/cancel/resume. Cache results; invalidate only when source content or algorithm changes. Never decode an entire collection concurrently. - **2026-09-21 done except explicit playback priority.** One streaming pass per file, one file at a time (a second job is refused), pause/resume/stop from the Audio analysis block, results saved per track so stopping loses nothing and running again resumes; a result is reused unless the file's size/mtime or `ALGORITHM_VERSION` changed (then shown as out of date). Memory is bounded by construction (onset envelope capped at 6 min, ~4 MB key excerpt). **Measured (release build, 5-min mono 44.1 kHz WAV): 1.9 s, about 157x realtime on one core; the whole test process peaked at 38 MB RSS.** Not done: lowering the worker thread's OS priority or throttling while the app is playing (only the single-worker cap protects playback today); not measured with a real 100k-track library or alongside live playback.
- [x] Show waveform and analysis status in details; add BPM range, key, loudness and analyzed/unanalyzed filters and sorting. - **2026-09-21 done.** Inspector shows the waveform and analysis; BPM, Key and Loudness columns (BPM/Key visible by default, Loudness hidden) are sortable; the Filters dialog has BPM range, key (any spelling), louder-than and analyzed/not analyzed. Selection toolbar "Analyze" and a library-wide "Analyze library / N remaining".
- [x] Build smart playlists as persisted all/any rules over catalog fields, ratings/tags, dates/play counts and analysis, with sort/limit and live reevaluation. Support saving a fixed snapshot. - **2026-09-21 done.** `smart_playlists.rs`: fixed set of fields and operators (text, number, between, within/not-within last N days incl. never-played, tag, key, analyzed), every value a bound parameter, invalid rules rejected at save. Results are never stored: every open/count re-evaluates. Editor dialog with a live match count while editing; play, shuffle, edit, delete (confirm), and "save a fixed copy" (an ordinary playlist). Rules and BPM/key corrections are in the catalog backup (analysis results are not; they are recomputed).
- [ ] Keep loudness analysis separate from gain application. If normalization playback is included, make it an explicit mode with clipping protection and verified gain handling. - **Separation is done** (loudness is only stored/shown/filtered; nothing changes playback gain). Normalization playback itself is **not built**; leave open until wanted.

**Exit demo:** analyze a selected album during uninterrupted playback, cancel/restart and reuse completed results. Correct a BPM estimate; a smart playlist such as “house, 120–128 BPM, rating ≥4, not played in 30 days” updates accordingly. Publish accuracy limits and CPU/native-memory results.

**Phase 5 status (2026-09-21): built, verification gaps listed.** Covered by tests: cancel then rerun reuses saved results (`cancel_stops_the_job_and_a_rerun_resumes_from_saved_results`), a changed file is re-analyzed, corrections beat tags beat estimates and undo, and `smart_playlist_exit_demo_house_120_to_128_rated_not_played_in_30_days_updates_live` (rating adds, a play removes, 40 quiet days re-adds, a BPM correction removes). Rust `cargo test --lib` 190 passed; `tahti-web` component/lib vitest green. **Not verified:** analysis during real playback (no audio device/display here), real-music accuracy, the new dialogs in the running app, and analysis on a large library. Accuracy limits are in the `analysis_dsp.rs` header; the key estimate on non-tonal audio (e.g. drums only) is a low-confidence guess by design.

## Phase 6 — Folder automation and performance hardening

Depends on: phases 1–5; profiling and bounded design apply from phase 0 onward.

- [ ] Add watched roots with debounced events, incremental reconciliation, missing-drive detection and fallback full rescan. **2026-09-21 watching shipped; rename/tag-edit handling open:** `local_library/watcher.rs` watches every root recursively (`notify`), restarted at startup and after add/remove/relink; events collapse behind a 3 s quiet period, then only the touched roots run the existing idempotent `scan_root` (new files imported, vanished marked missing, returned recovered), and a `library://roots-changed` event makes the panel refresh and toast. A 10-minute fallback full rescan covers dropped events and unmounted/remounted drives (a root that can't be watched is logged and left to that rescan). Overlapping reconciles are skipped. **Same day, second pass:** `reconcile.rs` — a vanished track and a newly seen file with the same file name and byte size, unique on both sides, are treated as a move (path updated, ID/playlists/overrides kept; ambiguous cases import as new); migration `0010_file_mtime.sql` stores each file's mtime, and a scan re-reads known files whose size or mtime changed (first scan only records mtime), reapplying user overrides; the scan result gained `moved`/`updated`. **Third pass:** a "Watch folders for changes" toggle in the Watched folders block pauses/resumes watching (persisted in new `library_settings`, migration `0011`; manual Rescan always works; also stops the 10-minute fallback while paused). **Fourth pass:** renames that also change the file name are now detected too — after the name+size pass, leftover vanished tracks and new files pair on identical byte size *and* mtime (a rename preserves both), one-to-one only, and only for tracks with a stored mtime (`reconcile::pair_renames`); ambiguous cases still import as new. Tests: 4 for root matching/event filtering, 4 for moves/re-read (`cargo test --lib local_library` 132/132); live event delivery not exercised here (no GUI). Handle rename/delete/external tag edits without losing playlist IDs or user overrides.
- [ ] Finish catalog query indexes, artwork cache limits, queue virtualization and scoped progress subscriptions. **2026-09-21 partial:** re-measured the 100k fixture (debug build, first page + page 500): artist sort 404 ms and album sort 329 ms were the outliers; migration `0012_sort_indexes.sql` adds ascending and descending expression indexes matching `order_clause` exactly, bringing both to ~11 ms (descending page 500 ~6 ms, asserted <100 ms in the ignored 100k test). Remaining sorts are 100-200 ms unindexed, judged acceptable (facets: albums 340 ms, artists 105 ms). Queue: `QueuePanel` rows outside the viewport now skip layout/paint above 100 items (`content-visibility: auto`), a deliberate alternative to real virtualization, which would break dnd-kit reorder/auto-scroll across the queue and could not be verified without a display; true windowing stays open. Artwork cache limits: not applicable yet, there is no artwork cache (embedded artwork extraction is still open in Phase 1). Scoped progress subscriptions: only `DesktopLibraryPanel` listens to the import/roots events, so nothing to scope today. Coordinate shared fixes with [performance workplan](player-performance-optimizations.md) rather than duplicating them.
- [ ] Profile native CPU and RSS/PSS across import, search, playback and analysis; repeat a 30-minute soak and repeated view/preset/track changes. Explain retained caches versus unbounded growth; validate cancellation releases job resources. **2026-09-21 partial (native library side):** ignored harness `local_library/profile_tests.rs` (`profile_native`, `profile_soak`; release build, `TRACKS`/`SOAK_SECS`, memory from `/proc/self/smaps_rollup`). Results, 3,000-file fixture (60 are 20 s kick tracks), Linux, this dev machine: import 3,000 files 0.37 s wall / 0.48 s cpu, RSS 10 -> 29 MB; idempotent rescan 42 ms; search 2.1 ms average; analysis 60 x 20 s tracks 11.5 s (~105x realtime, one core), RSS 30 -> 79 MB; cancelling a re-analysis returned in 150 ms with `cancelled=true` and no RSS growth. **30-minute soak** (31,063 cycles of rescan + 5 searches + touched-file re-read + 3-track forced analysis): RSS/PSS 26/24 MB at start, 55/52 MB after the first minute (SQLite 32 MB page cache + analysis buffers filling), then 58/55 at 8 min and 59/57 at 30 min — a bounded plateau (~+4 MB over the last 29 min, flattening), not linear growth. Still open: playback, WebView/GPU process memory, view/preset/track-change soak and frontend traces (need the GUI app on a display).
- [ ] Validate target-platform format playback, Unicode/removable-drive behavior, keyboard navigation, migration recovery and offline startup. Record supported/tested platforms and formats explicitly. **2026-09-21 partial:** tested on Linux only. New tests: a root on an unplugged drive (Unicode folder and file names) keeps its tracks, marks them missing and recovers with the same IDs when the folder returns; reopening the on-disk catalog file keeps its data and re-running migrations is a no-op (offline startup needs nothing but that file); earlier tests cover Unicode paths and import of FLAC/WAV, the only imported formats. Still open: Windows/macOS runs, real-device playback per format, keyboard navigation pass, corrupt-database/failed-migration recovery.

**Exit demo:** add/change/move files externally and see incremental reconciliation; 10k-track real workflow and 100k-row stress fixture meet recorded budgets without playback regressions. Publish before/after traces and known limitations.

## Phase 7 — Optional acquisition and cloud connections

Depends on: stable core phases 1–6. These do not gate personal library use.

- [ ] Soulseek native add-on: help/settings copy, Configure → test → save/enable, native connectivity, search/download progress/cancel, then completed downloads through the same importer. Tahti servers do not relay searches or P2P traffic. Follow plugin/registry checklist when implementing.
- [ ] Optional managed-copy/organize-files workflow with destination preview, collision handling and explicit confirmation before changing originals.
- [ ] Optional local-to-cloud publishing/linking via actual sibling API contracts; keep local paths/private metadata off the server by default. Upload is an explicit action, separate from import.
- [ ] Revisit custom title-bar default, installer branding and optional desktop status-bar shortcuts after catalog delivery.

## Delivery and completion rules

Each phase is a reviewable slice with its exit demo; no calendar estimate until phase 0 confirms native dependencies. Phases 0–3 deliver the everyday desktop library; phases 4–6 deliver pro catalog/analysis quality. Phase 7 is optional follow-up.

Before UI implementation inspect Storybook surfaces and applicable component/native/testing instructions. Update VIEW-CATALOG for real route/story/nav changes. Add behavioral tests for migration/restart, file identity, query pagination, local playlist round-trips and job cancellation; run package/native checks appropriate to each slice. No commits/push/deploy without user instruction.

Track open steps here; fold completed phase summaries into HISTORY and remove completed task bullets. When the core plan is complete, move any remaining optional work to explicit follow-up todos, then close this file and its INDEX/WORKPLAN entries.
